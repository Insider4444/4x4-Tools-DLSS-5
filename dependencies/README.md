# Build dependencies

Obtain the Adobe After Effects SDK and NVIDIA DLSS SDK from their respective providers. Set AE_SDK_BASE_PATH, DLSS_SDK_ROOT and DLSSNR_RUNTIME_DLL, or pass the equivalent arguments to scripts/build.ps1. SDK headers, libraries and tools are intentionally excluded from this repository. See docs/BUILDING.md for the complete procedure and tested versions.
